<% p = @page || @channel || @site || @product %>
<script>
  window._bd_share_config = {
    common : {
      <% if p = @page || @channel %>
        bdText : '<%= p.title %>',
        bdDesc : '<%= p.description %>',
        bdUrl : '<%= get_cms_url(p) %>',
        bdPic : '<%= p.image_path %>',
      <% elsif p = @site %>
        bdText : '<%= p.title %>',
        bdDesc : '<%= p.description %>',
        bdUrl : '<%= frontend_site_url(p) %>',
        bdPic : '<%= p.image_items.first %>',
      <% elsif p = @product %>
        bdText : '<%= p.name %>',
        bdDesc : '<%= p.description %>',
        bdUrl : '<%= frontend_product_url(p) %>',
        bdPic : '<%= p.image_items.first %>',
      <% end %>
      bdMini: 2,
      bdStyle: 1
    },
    share : [{
      "bdSize" : 16
    }],
    // image : [{
    //   viewType : 'list',
    //   viewPos : 'top',
    //   viewColor : 'black',
    //   viewSize : '16',
    //   viewList : ['qzone','tsina','sqq','weixin']
    // }],
    // selectShare : [{
    //   "bdselectMiniList" : ['qzone','tsina','sqq','weixin']
    // }]
  }
  with(document)0[(getElementsByTagName('head')[0]||body).appendChild(createElement('script')).src='http://bdimg.share.baidu.com/static/api/js/share.js?v=89860593.js?cdnversion='+~(-new Date()/36e5)];
</script>
