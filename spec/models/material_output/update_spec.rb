require 'rails_helper'

RSpec.describe MaterialOutput::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_output)
  #   flag, new_record = MaterialOutput::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_output)
  #   record2 = create(:material_output2)
  #   attrs = attributes_for(:material_output2)
  #   flag, _ = MaterialOutput::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
