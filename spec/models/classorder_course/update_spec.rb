require 'rails_helper'

RSpec.describe ClassorderCourse::Create, type: :model do

  # it 'for name' do
  #   record = create(:classorder_course)
  #   flag, new_record = ClassorderCourse::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:classorder_course)
  #   record2 = create(:classorder_course2)
  #   attrs = attributes_for(:classorder_course2)
  #   flag, _ = ClassorderCourse::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
