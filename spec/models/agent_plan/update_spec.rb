require 'rails_helper'

RSpec.describe AgentPlan::Create, type: :model do

  # it 'for name' do
  #   record = create(:agent_plan)
  #   flag, new_record = AgentPlan::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:agent_plan)
  #   record2 = create(:agent_plan2)
  #   attrs = attributes_for(:agent_plan2)
  #   flag, _ = AgentPlan::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
