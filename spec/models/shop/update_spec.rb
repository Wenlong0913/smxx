require 'rails_helper'

RSpec.describe Shop::Create, type: :model do

  # it 'for name' do
  #   record = create(:shop)
  #   flag, new_record = Shop::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:shop)
  #   record2 = create(:shop2)
  #   attrs = attributes_for(:shop2)
  #   flag, _ = Shop::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
