require 'rails_helper'

RSpec.describe Teacher::Create, type: :model do

  # it 'for name' do
  #   record = create(:teacher)
  #   flag, new_record = Teacher::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:teacher)
  #   record2 = create(:teacher2)
  #   attrs = attributes_for(:teacher2)
  #   flag, _ = Teacher::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
