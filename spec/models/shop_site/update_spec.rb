require 'rails_helper'

RSpec.describe ShopSite::Create, type: :model do

  # it 'for name' do
  #   record = create(:shop_site)
  #   flag, new_record = ShopSite::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:shop_site)
  #   record2 = create(:shop_site2)
  #   attrs = attributes_for(:shop_site2)
  #   flag, _ = ShopSite::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
