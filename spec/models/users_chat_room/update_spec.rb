require 'rails_helper'

RSpec.describe UsersChatRoom::Create, type: :model do

  # it 'for name' do
  #   record = create(:users_chat_room)
  #   flag, new_record = UsersChatRoom::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:users_chat_room)
  #   record2 = create(:users_chat_room2)
  #   attrs = attributes_for(:users_chat_room2)
  #   flag, _ = UsersChatRoom::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
